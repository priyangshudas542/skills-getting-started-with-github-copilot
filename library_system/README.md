Library System
================

Small library management package with JSON-backed storage.

Usage examples:

- Add a book: `python -m library_system.cli add-book "Title" "Author" --copies 2`
- Add a member: `python -m library_system.cli add-member "Alice"`
- Borrow: `python -m library_system.cli borrow 1 1`
